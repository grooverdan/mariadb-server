#!/bin/bash

set -e

# Install DuckDB UDFs
mariadb --defaults-file=/etc/mysql/debian.cnf < /usr/share/mariadb/duckdb/install.sql || true
# Always exit with success instead of leaving dpkg in a broken state


#DEBHELPER#
