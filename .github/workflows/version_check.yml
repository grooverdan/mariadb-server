name: version check
on:
   pull_request:
      branches:
         - "[1-9][0-9].[0-9]+"
      paths:
        - VERSION
jobs:
  version_check:
     if: github.actor != 'dbart'
     runs-on: ubuntu-latest
     steps:
        - run: false
